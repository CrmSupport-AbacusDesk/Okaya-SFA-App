#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../@ionic/v1-toolkit/bin/ionic-v1" "$@"
else 
  exec node  "$basedir/../@ionic/v1-toolkit/bin/ionic-v1" "$@"
fi
